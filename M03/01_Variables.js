//********** VARIABLES **********

var cajon; // ==> Primero se define.

cajon = 'Bufanda verde'; // ==> En seugundo lugar, se le asigna un valor (inicialización).

//También se puede simplificar de la siguiente manera:

var cajon1 = 'Media azul'; // ==> aquí se define y se inicializa la variable en la misma linea.

//********** TIPOS DE DATOS (más comunes)**********

// --> String:

var bootcamp = 'Henry';

// --> Numeros:

var numeroEntero = 17;

var numeroNegativo = -4;

var numeroDecinal = 11.75;

// --> Boleanos:

var estoyEntendiendo = true;

var estoyAburrido = false;

// --> Undefined:

var cajon2; // ==> Se define la variable, pero no se le asigna un valor.

console.log(cajon2); // ==> Como la variable no tiene un valor asignado, aparece < Undefined >.

// --> Null: En este caso es el programador el que indica que no hay valor asignado.

var sinNada = null;

//********** CANTIDAD DE CARACTERES DE UN STRING **********

"String".length //6